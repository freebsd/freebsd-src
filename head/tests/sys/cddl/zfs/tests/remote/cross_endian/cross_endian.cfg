#
# CDDL HEADER START
#
# The contents of this file are subject to the terms of the
# Common Development and Distribution License (the "License").
# You may not use this file except in compliance with the License.
#
# You can obtain a copy of the license at usr/src/OPENSOLARIS.LICENSE
# or http://www.opensolaris.org/os/licensing.
# See the License for the specific language governing permissions
# and limitations under the License.
#
# When distributing Covered Code, include this CDDL HEADER in each
# file and include the License file at usr/src/OPENSOLARIS.LICENSE.
# If applicable, add the following below this CDDL HEADER, with the
# fields enclosed by brackets "[]" replaced with your own identifying
# information: Portions Copyright [yyyy] [name of copyright owner]
#
# CDDL HEADER END
#

#
# Copyright 2008 Sun Microsystems, Inc.  All rights reserved.
# Use is subject to license terms.
#
# ident	"@(#)cross_endian.cfg	1.4	08/08/15 SMI"
#

. ${STF_TOOLS}/contrib/include/logapi.kshlib

export TESTSNAP=testsnap.${TESTCASE_ID}
export FILESZ=100m
export TESTFILE1=testfile1.${TESTCASE_ID}
export TESTFILE2=testfile2.${TESTCASE_ID}
export TESTFILE3=testfile3.${TESTCASE_ID}
export TESTFILE4=testfile4.${TESTCASE_ID}
export TESTFILE5=testfile5.${TESTCASE_ID}
export TESTFILE6=testfile6.${TESTCASE_ID}
export TESTFILE7=testfile7.${TESTCASE_ID}
export TESTFILE8=testfile8.${TESTCASE_ID}

export NORMALPOOL=poolnormal.${TESTCASE_ID}
export MIRRORPOOL=poolmirror.${TESTCASE_ID}
export RAIDZPOOL=poolraidz.${TESTCASE_ID}
export RAIDZ2POOL=poolraidz2.${TESTCASE_ID}
export RTESTPOOL=rtestpool.${TESTCASE_ID}
export RTESTFS=rtestfs.${TESTCASE_ID}
export RTESTFILE=rtestfile.${TESTCASE_ID}

# Among all given remote hosts, we only select one same arch machine and
# one of each different arch type machines for testing.
TESTHOSTS=""
diff_archs=""
typeset -i same_found=0
typeset -i diff_found=0
l_arch=`uname -m`
for rhost in $RHOSTS; do
        r_arch=`$RSH -n $rhost "uname -m"`
        if [[ "$r_arch" != "$l_arch" ]]; then
                if (( ${#diff_archs} == 0 )); then
                        TESTHOSTS="$TESTHOSTS $rhost"
                        diff_archs=$r_arch
                        continue
                else
			diff_found=0
                        for eacharch in $diff_archs; do
                                if [[ "$r_arch" == "$eacharch" ]]; then
					diff_found=1
					break
				fi
			done
			if (( $diff_found == 0 )); then
                        	TESTHOSTS="$TESTHOSTS $rhost"
                                diff_archs="$diff_archs $r_arch"
                        fi
                fi
        elif (( $same_found == 0 )); then
                TESTHOSTS="$TESTHOSTS $rhost"
                same_found=1
                continue
        fi
done
export TESTHOSTS

