# $FreeBSD$

.include <bsd.own.mk>

TESTSDIR= ${TESTSBASE}/sys

TESTS_SUBDIRS+=		cddl
TESTS_SUBDIRS+=		dtrace
TESTS_SUBDIRS+=		geom
TESTS_SUBDIRS+=		kern
TESTS_SUBDIRS+=		net
TESTS_SUBDIRS+=		netinet
TESTS_SUBDIRS+=		opencrypto

.include <bsd.test.mk>
