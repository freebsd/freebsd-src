# $FreeBSD$

.PATH:	${.CURDIR}/../loader

LOADER=		zfsloader
NEWVERSWHAT=	"ZFS enabled bootstrap loader" i386
LOADER_ZFS_SUPPORT=yes
LOADER_ONLY=	yes
NO_MAN=		yes

.include "${.CURDIR}/../loader/Makefile"

