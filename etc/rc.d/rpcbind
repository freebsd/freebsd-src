#!/bin/sh
#
# $FreeBSD: src/etc/rc.d/rpcbind,v 1.12.2.1.2.1 2008/10/02 02:57:24 kensmith Exp $
#

# PROVIDE: rpcbind
# REQUIRE: NETWORKING ntpdate syslogd named

. /etc/rc.subr

name="rpcbind"
rcvar=`set_rcvar`
command="/usr/sbin/${name}"

load_rc_config $name
run_rc_command "$1"
