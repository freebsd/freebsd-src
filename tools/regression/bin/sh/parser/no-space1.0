# $FreeBSD: src/tools/regression/bin/sh/parser/no-space1.0,v 1.1.2.2.2.1 2012/03/03 06:15:13 kensmith Exp $

# These are ugly but are required to work.

set -e

while(false)do(:)done
if(false)then(:)fi
if(false)then(:)else(:)fi
(:&&:)||:
until(:)do(:)done
case x in(x);;*)exit 1;(:)esac
case x in(x);;*)exit 1;;esac
for i do(:)done
{(:)}
f(){(:)}
:|:
(:)|(:)
