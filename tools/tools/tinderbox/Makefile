# $FreeBSD: src/tools/tools/tinderbox/Makefile,v 1.5 2003/05/16 12:11:13 des Exp $

BINDIR		?= ${HOME}/bin
BINOWN		?= ${USER}
BINGRP		?= ${USER}
SCRIPTS		 = tbmaster.pl tinderbox.pl

MANDIR		?= ${HOME}/man/man
MANOWN		?= ${USER}
MANGRP		?= ${USER}
MAN		 = tbmaster.1 tinderbox.1

SUBDIR		 = www

.include <bsd.prog.mk>
