# $FreeBSD: src/tools/tools/hcomp/Makefile,v 1.1.26.1 2008/10/02 02:57:24 kensmith Exp $

BINDIR		?= ${HOME}/bin
BINOWN		?= ${USER}
BINGRP		?= ${USER}
SCRIPTS		 = hcomp.pl

MANDIR		?= ${HOME}/man/man
MANOWN		?= ${USER}
MANGRP		?= ${USER}
MAN		 = # empty

.include <bsd.prog.mk>
