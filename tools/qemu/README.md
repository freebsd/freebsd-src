# Scripts for use with QEMU

For bsd-user in qemu, it is desireable to have tables generated from the source
tree. This directory has the collection of those scripts that need the full
source tree to generate. They may graduate to qemu's repo if/when all the
supported releases have the data in a normal install.
