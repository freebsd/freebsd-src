# $FreeBSD$

PACKAGE=	tests
FILESGROUPS=	TESTS
TESTSPACKAGE=	${PACKAGE}

TAP_TESTS_SH=	legacy_test

FILESDIR=	${TESTSDIR}
FILES=		regress.base64.in
FILES+=		regress.out
FILES+=		regress.sh
FILES+=		regress.traditional.in

.include <bsd.test.mk>
