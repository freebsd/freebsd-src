# $FreeBSD$

ATF_TESTS_SH+=    	basic2_test
NETBSD_ATF_TESTS_SH=    basic

FILES+=	across.out
FILES+=	d_basic.in
FILES+=	d_basic.out
FILES+=	merge.out
FILES+=	other.in
FILES+=	threecol.out

BINDIR=	${TESTSDIR}

.include <netbsd-tests.test.mk>

.include <bsd.test.mk>
