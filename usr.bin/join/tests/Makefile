# $FreeBSD$

PACKAGE=	tests
FILESGROUPS=	TESTS
TESTSPACKAGE=	${PACKAGE}

TAP_TESTS_SH=	legacy_test

FILESDIR=	${TESTSDIR}
FILES=		regress.1.in
FILES+=		regress.2.in
FILES+=		regress.out
FILES+=		regress.sh

.include <bsd.test.mk>
