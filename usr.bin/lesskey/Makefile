# Makefile for lesskey
#
# $FreeBSD$

PROG=	lesskey
SRCS=	lesskey.c version.c

MAN1=	lesskey.1
MANDEPEND=	lesskey.1
CLEANFILES+=	${MANDEPEND}

.include "${.CURDIR}/../less/Makefile.common"
.include <bsd.prog.mk>
