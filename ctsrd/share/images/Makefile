# $FreeBSD$

FILES=	booting.png \
	upgrade-complete.png \
	upgrade-inprogress.png

NO_OBJ=
BINDIR?=        ${SHAREDIR}
BINOWN=         ${SHAREOWN}
BINGRP=         ${SHAREGRP}
FILESDIR=	${BINDIR}/images

.include <bsd.prog.mk>
