# $FreeBSD$

.PATH: ${BSDSRCTOP}/usr.sbin/fdread

PROG = fdcontrol

SRCS = \
	fdcontrol.c \
	fdutil.c

WARNS ?= 6

CFLAGS += -I${BSDSRCTOP}/usr.sbin/fdread

.if ${MACHINE} == "pc98"
CFLAGS += -DPC98
.endif

.include <bsd.prog.mk>
