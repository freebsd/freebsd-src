#
# CHERI -- Kernel for the SRI/Cambridge "CHERI" (Capability Hardware Enhanced
# RISC Instructions) FPGA soft core, as configured in its Terasic DE-4
# reference configuration.
#
# $FreeBSD$
#

# Ensure the kernel picks up this FDT
options         FDT_DTB_STATIC_ONLY

include		BERI_DE4_BENCHMARK
ident		CHERI_DE4_BENCHMARK

nocpu		CPU_BERI
cpu		CPU_CHERI

#options       CPU_CHERI_CHERI0
#options       CPU_CHERI_CHERI8
#options       CPU_CHERI_CHERI16
#options       CPU_CHERI_HW_CCALL_CHECKS
#options       CPU_CHERI_HW_RCLEAR
#options       CPU_CHERI_HW_CCLEAR
