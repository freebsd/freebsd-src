PROG   = htags
BINDIR = /usr/bin
MANDIR = /usr/man

all:
install:
	cp $(PROG).pl $(BINDIR)/$(PROG)
	chmod 755 $(BINDIR)/$(PROG)
	cp $(PROG).1 $(MANDIR)/man1
	chmod 644 $(MANDIR)/man1/$(PROG).1
clean:
