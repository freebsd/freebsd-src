Things to be added:

@ modules:
@ application:

	use of
		'user' = user to become,
		'uid' = user requesting service
		'euid' = privilege of current process.

@ sysadmin:

	included modules:
		behavior
	non-included modules:
		behavior/pointers.
