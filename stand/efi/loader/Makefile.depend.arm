# $FreeBSD$
# Autogenerated - do NOT edit!

DIRDEPS = \
	bin/sh.host \
	stand/efi/fdt \
	stand/efi/libefi \
	stand/fdt \
	stand/libsa \
	usr.bin/awk.host \


.include <dirdeps.mk>

.if ${DEP_RELDIR} == ${_DEP_RELDIR}
# local dependencies - needed for -jN in clean tree
.endif
