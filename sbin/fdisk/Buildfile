# $FreeBSD$

.PATH:	${BSDSRCTOP}/sys/geom

PROG =	fdisk

SRCS = \
	fdisk.c \
	geom_mbr_enc.c

WARNS ?=	4

LDADD += -lgeom -lbsdxml -lsbuf

ONLY_MACHINE = ${MACHINE_LIST:Npc98}

.include <bsd.prog.mk>

test:	${PROG}
	sh ${.CURDIR}/runtest.sh
