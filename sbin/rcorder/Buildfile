# $FreeBSD$

PROG = rcorder

SRCS = \
	ealloc.c \
	hash.c \
	rcorder.c

WARNS ?= 6

LDADD += -lutil

CFLAGS += -DORDER -I.

GENHDRS += util.h
util.h:
	cp ${BSDSRCTOP}/lib/libutil/libutil.h ${.TARGET}

.include <bsd.prog.mk>
