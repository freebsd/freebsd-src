# $FreeBSD$

MOUNT = ${BSDSRCTOP}/sbin/mount
UMNTALL= ${BSDSRCTOP}/usr.sbin/rpc.umntall

.PATH: ${MOUNT} ${UMNTALL}

PROG = mount_nfs

SRCS = \
	getmntopts.c \
	mount_nfs.c \
	mounttab.c

WARNS ?= 3

CFLAGS += -DNFS -I${MOUNT} -I${UMNTALL}

.include <bsd.prog.mk>
