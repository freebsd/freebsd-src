# $FreeBSD$

.PATH: ${BSDSRCTOP}/sbin/geom/misc

PROG = geom

SRCS = \
	geom.c \
	subr.c

NO_SHARED =

CFLAGS += -DCLASS_DIR=\"${CLASS_DIR}\"
CFLAGS += -I${.CURDIR} -I${.CURDIR}/..

LDADD = -lgeom -lsbuf -lbsdxml -lutil

SRCDIRDEP= sbin/geom/misc
DIRDEP= gnu/lib/csu gnu/lib/libgcc include include/machine lib/csu/MACHINE_ARCH lib/libc lib/libexpat lib/libgeom lib/libsbuf lib/libutil stage sys/MACHINE_ARCH/include sys/geom sys/sys
.include <bsd.prog.mk>
