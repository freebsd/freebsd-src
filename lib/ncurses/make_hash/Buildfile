# $FreeBSD$

.PATH: ${NCURSES_DIR}/ncurses/tinfo

BINDIR = ${STAGEDIR}/buildtools/ncurses

PROG = make_hash

SRCS = comp_hash.c

HOSTPROG =


CFLAGS +=  -DMAIN_PROGRAM
BUILTFILES = hashsize.h

hashsize.h:
	sh ${NCURSES_DIR}/include/MKhashsize.sh ${NCURSES_DIR}/include/Caps > ${.TARGET}

.include <bsd.prog.mk>
