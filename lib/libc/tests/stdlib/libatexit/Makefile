SHLIB_CXX=		libatexit
SHLIB_NAME=		libatexit.so
SHLIB_MAJOR=		1
SHLIBDIR=	${TESTSDIR}
PACKAGE=	tests
SRCS=		libatexit.cc

TESTSDIR:=	${TESTSBASE}/${RELDIR:C/libc\/tests/libc/:H}


.include <bsd.lib.mk>
