# $FreeBSD$

.include <bsd.own.mk>

FILESGROUPS=	FILES
PACKAGE=	tests
FILESGROUPS+=	TESTS
TESTSPACKAGE=	${PACKAGE}
TESTS+=		Kyuafile

SUBDIR=		tests

.PATH:		${SRCTOP}/tests
KYUAFILE=	yes

.include <bsd.test.mk>
